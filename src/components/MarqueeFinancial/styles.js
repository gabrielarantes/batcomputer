import styled from 'styled-components/native';
import {colors} from '../../constants/colors';

export const ViewMarquee = styled.View`
  flex: 0.02;
  background-color: ${colors.black};

  padding-top: 25px;
  padding-bottom: 5px;
  padding-left: 15px;
  padding-right: 15px;
`;
